import { test } from "node:test";

test(
  "bp reconcile receipt fixtures/bad/mazur.bad-gradient.jsonl fails Rule 4 on w5",
  { todo: "Implemented in commit: Implement receipt reconciler Rule 4 failure proof" }
);
